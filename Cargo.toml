[package]
name = "meshbool"
version = "0.0.0"
edition = "2024"

[features]

[dependencies]
manifold-rs = "0.6"
nalgebra = { version = "0.34", default-features = false, features = ["std"] }

[dev-dependencies]
approx = "0.5"
